<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <title>Dashboard Admin</title>
</head>
<body>
  <h1>Bem-vindo ao painel administrativo</h1>

  <% if (user) { %>
    <p>Olá, <strong><%= user.nome %></strong>!</p>
    <p>Seu e-mail é <strong><%= user.email %></strong>.</p>
    <p>Você está logado como <strong><%= user.tipo %></strong>.</p>
  <% } else { %>
    <p style="color: red;">Usuário não encontrado na sessão.</p>
  <% } %>
</body>
</html>